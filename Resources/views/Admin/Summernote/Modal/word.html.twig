<div class="modal" data-modal="word">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close">
                    <i class="fa fa-close"></i>
                </button>
                <h4 class="modal-title">Insertion d'un document Word</h4>
            </div>
            <div class="modal-body">
                <select style="width: 100%">
                    <option></option>
                    {% for word in words %}
                        <option value="{{ word.id }}">{{ word.fileName }}</option>
                    {% endfor %}
                </select>
                <div class="text-center">
                    <span>ou</span>
                    <br>
                <span class="btn btn-success fileinput-button">
                    <i class="fa fa-plus"></i>
                    <span>Ajouter un nouveau document Word</span>
                    <input name="mediaFile[file]" type="file">
                </span>
                </div>
            </div>
        </div>
    </div>
</div>
